// This file is kept for reference but not used.
// Score persistence is handled by the useScores hook in client components.

export type ScoreData = {
  score: number;
  date: string;
  guesses: number;
  word: string;
};
